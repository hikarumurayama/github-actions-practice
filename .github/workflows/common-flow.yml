name: "common-flow"

on:
  workflow_call:
    inputs:
        statement:
          required: true
          type: string

jobs:
    name: "common workflow job"
    runs-on: ubuntu-latest
    steps:
      - name: execution
        run: |
          echo ${{inputs.statement}}
